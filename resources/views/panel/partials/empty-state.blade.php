<div class="rounded-lg border border-dashed border-slate-300 bg-white p-8 text-center">
    <h3 class="text-sm font-semibold text-slate-950">{{ $title ?? 'No records' }}</h3>
    <p class="mt-2 text-sm text-slate-600">{{ $message ?? 'There is nothing to show yet.' }}</p>
</div>
